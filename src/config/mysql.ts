import { PoolOptions } from "mysql2"

const access: PoolOptions = {
    host: 'localhost',
    port: 3306,
    user: 'root',
    password: 'tisheel.2004.21',
    database: 'tasky'
}

export default access